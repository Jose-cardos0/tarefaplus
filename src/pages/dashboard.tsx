import Head from "next/head";

export default function Dashboard() {
  return (
    <div>
      <Head>
        <title>Tarefas+ | Painel de tarefas</title>
      </Head>
      <h1>Dashboard</h1>
    </div>
  );
}
